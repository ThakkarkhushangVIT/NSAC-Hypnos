import datetime
def launch_datetime():
    year = int(input('Enter a year'))
    month = int(input('Enter a month'))
    day = int(input('Enter a day'))
    date = datetime.date(year, month, day)
    hour=int(input('enter the launch hour'))
    minutes=int(input('enter the minutes'))
    seconds=int(input('enter the seconds'))
    time=datetime.time(hour,minutes,seconds)
def landing_datetime():
    year = int(input('Enter a year'))
    month = int(input('Enter a month'))
    day = int(input('Enter a day'))
    date = datetime.date(year, month, day)
    hour=int(input('enter the launch hour'))
    minutes=int(input('enter the minutes'))
    seconds=int(input('enter the seconds'))
    time=datetime.time(hour,minutes,seconds)
def docking_datetime():
    year = int(input('Enter a year'))
    month = int(input('Enter a month'))
    day = int(input('Enter a day'))
    date = datetime.date(year, month, day)
    hour=int(input('enter the launch hour'))
    minutes=int(input('enter the minutes'))
    seconds=int(input('enter the seconds'))
    time=datetime.time(hour,minutes,seconds)
def undocking_datetime():
    year = int(input('Enter a year'))
    month = int(input('Enter a month'))
    day = int(input('Enter a day'))
    date = datetime.date(year, month, day)
    hour=int(input('enter the launch hour'))
    minutes=int(input('enter the minutes'))
    seconds=int(input('enter the seconds'))
    time=datetime.time(hour,minutes,seconds)
def crewmembernutrition_schedule():
    n=int(input('enter the no of meals in a day'))
    i=0
    while(i<n):
        time
